import React, { useEffect } from "react";
import Image from "./Image";

const MainContainer = () => {
  return (
    <div className=" py-20 bg-gradient-to-r from-slate-400">
      <Image />
    </div>
  );
};

export default MainContainer;
